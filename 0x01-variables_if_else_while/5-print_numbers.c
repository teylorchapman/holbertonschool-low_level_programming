#include<stdio.h>
/**
* main - Prints all signle digit numbers starting at 0.
*
* Return: Always 0
*/
int main(void)
{
int numero;
for (numero = 0; numero < 10; numero++)
printf("%d", numero);
printf("\n");
return (0);
}
